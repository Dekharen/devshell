name = "dev-shell"

stages = [
    "@base/debian",
    "@features/basic-debian",
    "@post/dev-user",
]

docker_args = [
    "--memory=4g",
    "--cpus=2",
]

[[volumes]]
host = "/home/dev/devshell"
container = "/workspace"
mode = "rw"